name: Update YouTube Music sections

on:
  workflow_dispatch:
  schedule:
    - cron: "15 */6 * * *"  # 6시간마다
  push:
    paths:
      - "scripts/render_ytmusic.py"

jobs:
  render:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: python -m pip install --upgrade pip ytmusicapi
      - name: Restore oauth.json from secret
        run: echo '${{ secrets.YTM_OAUTH_JSON }}' > oauth.json
      - name: Render README
        run: python scripts/render_ytmusic.py
      - name: Commit & Push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add README.md
          git diff --cached --quiet || git commit -m "chore(ytmusic): refresh README via CI"
          git push
